{
  "name": "traceview",
  "version": "0.1.0",
  "description": "Local, inspectable agent run viewer for OpenCode and Claude Code",
  "private": true,
  "license": "UNLICENSED",
  "type": "module",
  "engines": {
    "node": ">=22.13.0"
  },
  "scripts": {
    "start": "node cli.mjs serve",
    "test": "node --test tests/*.test.mjs"
  }
}
